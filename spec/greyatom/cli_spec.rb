require "spec_helper"

describe Greyatom::Cli do
  it "has a version number" do
    expect(Greyatom::Cli::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
